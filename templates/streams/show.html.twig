{% extends 'base.html.twig' %}

{% block title %}Streams{% endblock %}

{% block body %}
    <h1>Streams</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stream.id }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ stream.StartDate ? stream.StartDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ stream.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_streams_index') }}">back to list</a>

    <a href="{{ path('app_streams_edit', {'id': stream.id}) }}">edit</a>

    {{ include('streams/_delete_form.html.twig') }}
{% endblock %}
