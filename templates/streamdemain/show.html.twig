{% extends 'base.html.twig' %}

{% block title %}Streamdemain{% endblock %}

{% block body %}
    <h1>Streamdemain</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ streamdemain.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_streamdemain_index') }}">back to list</a>

    <a href="{{ path('app_streamdemain_edit', {'id': streamdemain.id}) }}">edit</a>

    {{ include('streamdemain/_delete_form.html.twig') }}
{% endblock %}
