<form method="post" action="{{ path('app_streamdemain_delete', {'id': streamdemain.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ streamdemain.id) }}">
    <button class="btn">Delete</button>
</form>
